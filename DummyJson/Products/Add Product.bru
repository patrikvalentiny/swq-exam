meta {
  name: Add Product
  type: http
  seq: 1
}

post {
  url: {{base_url}}/products/add
  body: json
  auth: inherit
}

body:json {
  {
      "title":"{{$randomProductName}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
