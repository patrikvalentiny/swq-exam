meta {
  name: Add Product
  type: http
  seq: 1
}

post {
  url: {{base_url}}/products/add
  body: json
  auth: inherit
}

body:json {
  {
      "title":"{{$randomProductName}}"
  }
}

assert {
  res.status: eq 201
  res.body.id: isNumber
}
